import { createTheme } from "@mui/material";

export const them = createTheme({
  breakpoints: {
    values: {
      xs: 0,
      sm: 600,
      md: 767,
      lg: 1263,
      xl: 1536,
    },
  },
});
